<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>予約キャンセル - 予約システム</title>
    <style>
        body {
            font-family: 'Noto Sans JP', Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 10px;
            font-size: 2em;
        }
        
        .subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 30px;
            font-size: 1.1em;
        }
        
        .cancel-methods {
            margin-bottom: 30px;
        }
        
        .method-card {
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }
        
        .method-card:hover {
            border-color: #667eea;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.1);
        }
        
        .method-card.active {
            border-color: #667eea;
            background-color: #f8f9ff;
        }
        
        .method-title {
            font-size: 1.2em;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }
        
        .method-title input[type="radio"] {
            margin-right: 10px;
            transform: scale(1.2);
        }
        
        .method-description {
            color: #666;
            margin-bottom: 15px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #333;
        }
        
        input[type="text"], input[type="email"] {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s ease;
            box-sizing: border-box;
        }
        
        input[type="text"]:focus, input[type="email"]:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        
        .cancel-form {
            display: none;
        }
        
        .cancel-form.active {
            display: block;
            animation: slideIn 0.3s ease;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 10px;
        }
        
        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }
        
        .button.danger {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
        }
        
        .button.danger:hover {
            box-shadow: 0 5px 15px rgba(255, 107, 107, 0.3);
        }
        
        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #667eea;
            text-decoration: none;
            font-weight: bold;
        }
        
        .back-link:hover {
            text-decoration: underline;
        }
        
        .warning {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            color: #856404;
        }
        
        .info {
            background-color: #d1ecf1;
            border: 1px solid #b8daff;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            color: #0c5460;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚫 予約キャンセル</h1>
        <p class="subtitle">予約のキャンセルをご希望の方は、以下のいずれかの方法でお手続きください</p>
        
        <div class="warning">
            <strong>⚠️ ご注意:</strong> キャンセルされた予約は元に戻すことができません。
        </div>
        
        <div class="cancel-methods">
            <div class="method-card" id="code-method">
                <div class="method-title">
                    <input type="radio" name="cancel-method" value="code" id="method-code">
                    <label for="method-code">予約コードでキャンセル</label>
                </div>
                <div class="method-description">
                    予約完了時に発行された予約コードをお持ちの方はこちら
                </div>
            </div>
            
            <div class="method-card" id="email-method">
                <div class="method-title">
                    <input type="radio" name="cancel-method" value="email" id="method-email">
                    <label for="method-email">メールアドレスでキャンセル</label>
                </div>
                <div class="method-description">
                    予約時に登録されたメールアドレスから予約を検索してキャンセル
                </div>
            </div>
        </div>
        
        <!-- 予約コードでキャンセル -->
        <form action="/cancel" method="POST" class="cancel-form" id="code-form">
            <div class="info">
                予約コードは予約完了画面およびQRコードに表示されている8桁の英数字です。
            </div>
            <div class="form-group">
                <label for="reservation_code">予約コード</label>
                <input type="text" id="reservation_code" name="reservation_code" placeholder="例：ABC12345" required maxlength="8" pattern="[A-Za-z0-9]{8}">
            </div>
            <button type="submit" class="button danger">この予約をキャンセルする</button>
        </form>
        
        <!-- メールアドレスでキャンセル -->
        <form action="/cancel-by-email" method="POST" class="cancel-form" id="email-form">
            <div class="info">
                登録されたメールアドレスに該当する予約一覧を表示します。
            </div>
            <div class="form-group">
                <label for="email">メールアドレス</label>
                <input type="email" id="email" name="email" placeholder="example@email.com" required>
            </div>
            <button type="submit" class="button">予約を検索する</button>
        </form>
        
        <a href="/" class="back-link">← トップページに戻る</a>
    </div>

    <script>
        // ラジオボタンの処理
        const methodRadios = document.querySelectorAll('input[name="cancel-method"]');
        const methodCards = document.querySelectorAll('.method-card');
        const cancelForms = document.querySelectorAll('.cancel-form');

        methodRadios.forEach(radio => {
            radio.addEventListener('change', function() {
                // すべてのカードとフォームを非アクティブにする
                methodCards.forEach(card => card.classList.remove('active'));
                cancelForms.forEach(form => form.classList.remove('active'));
                
                // 選択されたメソッドのカードとフォームをアクティブにする
                const selectedMethod = this.value;
                document.getElementById(`${selectedMethod}-method`).classList.add('active');
                document.getElementById(`${selectedMethod}-form`).classList.add('active');
            });
        });

        // カードクリックでラジオボタンを選択
        methodCards.forEach(card => {
            card.addEventListener('click', function() {
                const radio = this.querySelector('input[type="radio"]');
                if (radio) {
                    radio.checked = true;
                    radio.dispatchEvent(new Event('change'));
                }
            });
        });

        // 予約コードの大文字変換
        document.getElementById('reservation_code').addEventListener('input', function() {
            this.value = this.value.toUpperCase();
        });
    </script>
</body>
</html>